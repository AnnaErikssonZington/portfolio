import React, { Component } from 'react';
import Body from './Body';

export default class About extends Component {
    render() {
        return (
            <div>
                <Body />
                About
            </div>
        );
    }
}
